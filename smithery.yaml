# Smithery.ai configuration
startCommand:
    type: http
    configSchema:
        type: object
        required:
            - clientSecretsPath
        properties:
            clientSecretsPath:
                type: string
                title: "Client Secrets JSON"
                description: "Path to your client_secrets.json file"
